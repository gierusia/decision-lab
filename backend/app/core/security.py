# Этап 1 (Auth) добавит сюда:
# - хэширование паролей (passlib CryptContext)
# - create_access_token / decode_access_token (python-jose)
#
# Специально оставлено пустым на Этапе 0, чтобы не тащить логику раньше времени.
